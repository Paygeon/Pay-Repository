{
  "builds": [
    { "src": "gulp release", "use": "@now/static-build" }
  ]
}
{
  "builds": [
    {
      "src": "gulp release",
      "use": "@now/static-build",
      "config": { "outputDirectory": "dist" }
    }
  ]
}
